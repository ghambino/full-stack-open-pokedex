name: Hello World!

on:
  push:
    branches:
      - master

jobs:
  hello_world_job:
    runs-on: ubuntu-18.04
    steps:
      - name: Say hello
        run: |
          echo "Hello World!"
    steps: 
      - name: display date
        run: |
          date
    steps: 
      - name: directory content
        run: |
          ls -l

